[pytest]
junit_family = legacy
junit_suite_name = 'result.xml'
markers =
    unit: mark a test as a unit test - does not require running emod
    container: mark a test that requires the container platform to run EMOD
    tutorial: mark a test that runs a full tutorial script end-to-end (requires container platform)
